# Used when building the dev image (Dockerfile.dev): context = . (repo root).
# Main image uses context docker/ and docker/.dockerignore.

# Git and CI
.git
.github
.gitignore

# Docs and examples (not needed in image build context)
README.md
examples/

# Tests: not copied into dev image; repo is mounted at run time for make ci / make lint
tests/

# Local / artifacts
artifacts/
.env
*.log

# IDE / OS
.idea
.vscode
.DS_Store
