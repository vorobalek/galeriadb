# Copy to stack.env and set a strong password. Used by galera and mariadb-exporter.
GALERIA_ROOT_PASSWORD=change-me
GALERIA_PEERS=tasks.galera
GALERIA_CLUSTER_NAME=galera_cluster

# For mariadb-exporter: create user in MariaDB and set password here if needed.
# MYSQLD_EXPORTER_PASSWORD=...
